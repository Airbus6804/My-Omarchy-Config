-- Personal window rules (Omarchy 4 / Quattro).

o.window("org.gnome.Nautilus", { float = true })

-- Scratchpad examples (from workspaces.conf — uncomment to enable)
-- o.window("vesktop-scratchpad", { workspace = "special:scratchpad", silent = true })
-- o.window("spotify-scratchpad", { workspace = "special:scratchpad", silent = true })
